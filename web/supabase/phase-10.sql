-- ============================================================================
--  10차: 메시지 숨김 (신고 처리 시 내용 보존)
-- ============================================================================
alter table messages add column if not exists hidden_at timestamptz;
