-- ============================================================================
--  15차: 채팅 사진 첨부
-- ============================================================================

-- 이미지만 있는 메시지는 content = '' 로 저장 (content 는 여전히 not null)
alter table messages add column if not exists image_url text;
